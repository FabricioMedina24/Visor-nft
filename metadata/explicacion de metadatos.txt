{
  "id": "1",                           // Identificador del modelo o NFT.
  "rarity": "Legendary",               // Nivel de rareza (sirve para aplicar colores o valores por defecto).
  "frames_count": 5,                   // Cantidad de imágenes diferentes que tiene el lienzo (para el carrusel).
  "cycle_interval": 30000,             // Tiempo que tarda en cambiar a la siguiente imagen (en milisegundos, 30000 = 30 segundos).
  "background_image": "environments/bg_4.png", // Imagen de fondo que se ve detrás de la escena 3D.

  "transform": {
    "scale": 1.0,                      // Tamaño del cuadro (1.0 = tamaño normal, 2.0 = doble de grande).
    "rotation_y": 0,                   // Giro inicial del cuadro (de izquierda a derecha).
    "offset_y": 0.0                    // Ajuste de altura del cuadro (positivo sube, negativo baja).
  },

  "lighting": {
    "ambient_intensity": 0.5,          // Fuerza de la luz general de todo el entorno.
    "camera_light_intensity": 1.2,     // Fuerza de la luz frontal (como si la cámara tuviera un flash encendido).
    "bloom_strength": 0.5,             // Fuerza general del efecto de resplandor/brillo (Bloom).
    "bloom_radius": 0.2,               // Qué tan difuminado o expandido es ese resplandor.
    "bloom_threshold": 1.0             // Nivel mínimo de luz que necesita un objeto para empezar a brillar.
  },

  "particles": {
    "enabled": true,                   // Activa (true) o desactiva (false) las partículas que flotan.
    "count": 10,                       // Cantidad constante de partículas flotando.
    "color": "#ffeaba",                // Color de las partículas.
    "speed": 1.0,                      // Velocidad a la que se mueven.
    "size_factor": 0.0015              // Tamaño individual de cada partícula.
  },

  "canvas": {
    "emissive_color": "#ffeaba",       // Color base del brillo o magia que sale del lienzo.
    "transition_duration": 1500,       // Cuánto tarda en revelarse la pintura inicial (en milisegundos).
    "intro_spin_speed": 16,            // Cantidad/velocidad de vueltas que da el cuadro en su animación de entrada.
    "intro_bloom_strength": 3.0        // Fuerza del resplandor justo cuando el cuadro termina de dar vueltas.
  },

  "magic_transition": {
    "enabled": true,                   // Activa o desactiva el efecto especial al cambiar de una imagen a otra.
    "intensity": 3.0,                  // Fuerza de la luz/magia durante el cambio de imagen.
    "bloom_strength": 3.0,             // Nivel de resplandor extremo durante el cambio.
    "duration": 1500,                  // Cuánto dura la animación de cambiar de imagen (en milisegundos).
    "shake": 0.03,                     // Cuánto "tiembla" o vibra el cuadro durante el cambio (0.03 es muy sutil).
    "particle_burst": 100              // Cantidad de partículas que explotan de golpe al cambiar de imagen.
  },

  "camera": {
    "fov": 75,                         // Campo de visión de la cámara (efecto de lente, 75 es un valor estándar).
    "distance_factor": 0.9,            // Qué tan cerca inicia la cámara del cuadro (menor = más cerca).
    "auto_rotate_speed": 1.2,          // Velocidad a la que la cámara gira sola alrededor del cuadro.
    "inactivity_delay": 5000           // Tiempo que debes dejar de tocar la pantalla para que vuelva a girar sola (5000 = 5 seg).
  }
}